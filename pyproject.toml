[project]
name = "coupang-cart-agent"
version = "0.1.0"
description = "Shared foundation and contracts for the Coupang cart agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
